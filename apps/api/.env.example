# MongoDB-Verbindung (Atlas: mongodb+srv://<user>:<password>@<cluster>/...)
MONGODB_URI=

# Datenbankname (optional, Standard: inkink)
MONGODB_DB=inkink

# Erlaubte Origins für CORS (komma-separiert; credentials sind aktiviert)
CORS_ORIGINS=http://localhost:3000